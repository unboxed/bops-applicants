<li>
  <h2 class="moj-task-list__section">
    <span class="moj-task-list__section-number"><%= requests_order(@validation_requests) %></span>Confirm changes to your application's red line boundary
  </h2>
  <ul class="moj-task-list__items" id="validation-section">
    <% @validation_requests["data"]["red_line_boundary_change_requests"].each do |change_request| %>
      <li class="moj-task-list__item">
        <% if change_request["state"] == "open" %>
          <%= link_to "Red Line Boundary", edit_red_line_boundary_change_request_path(change_request["id"], planning_application_id: params["planning_application_id"], change_access_id: params["change_access_id"]), class: "moj-task-list__task-name" %>
        <% else %>
          <%= link_to "Red Line Boundary", red_line_boundary_change_request_path(change_request["id"], planning_application_id: params["planning_application_id"], change_access_id: params["change_access_id"]), class: "moj-task-list__task-name" %>
        <% end %>
        <% if change_request["state"] == 'open' %>
          <strong class="govuk-tag govuk-tag--grey" style="float: right;" id="eligibility-status">
            Not started
          </strong>
        <% else %>
          <strong class="govuk-tag app-task-list__tag" style="float: right;" id="eligibility-status">
            Complete
          </strong>
        <% end %>
      </li>
    <% end %>
  </ul>
</li>
